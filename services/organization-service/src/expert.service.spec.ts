import { ExpertService } from './expert.service';
describe('ExpertService',()=>{it('emits only approved public projection',async()=>{const rows=[{id:'11111111-1111-4111-8111-111111111111',createdAt:new Date(),displayName:'A',bio:null,country:'VN',language:'vi',visibility:'PUBLIC',userRef:'secret',version:1,organization:{id:'22222222-2222-4222-8222-222222222222',name:'Org',country:'VN'},expertises:[{expertise:{id:'33333333-3333-4333-8333-333333333333',slug:'ai',labels:{en:'AI'}}}]}]; const result=await new ExpertService({findPublic:jest.fn().mockResolvedValue(rows)} as never).list({limit:20}); expect(result.items[0]).not.toHaveProperty('userRef'); expect(result.items[0].expertises[0].slug).toBe('ai'); expect(result.nextCursor).toBeNull();});});
